select p.name, count(*) as count_of_orders, round(avg(m.price), 2) as average_price, max(m.price), min(m.price)
from pizzeria p 
left join menu m on m.pizzeria_id = p.id
left join person_order po on po.menu_id = m.id 
where po.order_date is not null
group by p.name
order by p.name;
